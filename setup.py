from setuptools import setup

setup(
    name='hazelpy',
    version='0.1',
    long_description=__doc__,
    packages=['hazelpy'],
    include_package_data=True,
    zip_safe=False,
    install_requires=[]
)
